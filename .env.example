# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# NEVER expose service role in frontend (no VITE_ prefix!)
# SUPABASE_SERVICE_ROLE=your-service-role-key (server-side only)

# Exchange Rate API (optional — falls back to hardcoded rates)
# VITE_EXCHANGE_RATE_API_KEY=your-api-key

# Domain & Environment
VITE_DOMAIN=https://www.beauzead.shop
VITE_ENVIRONMENT=production

# Stripe — REQUIRED for payments
# Frontend (VITE_ prefix = exposed to browser — publishable key only!)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
# Server-side only (NO VITE_ prefix — used by Vite dev middleware & Edge Functions)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
